# Assets

This folder contains small sample media files used to validate the hardware pipeline.

## `sample.gif`
Copy `assets/sample.gif` to either:
- your microSD card as `gifs/sample.gif` (Arduino: `/gifs/sample.gif` on the card), or
- the `CIRCUITPY` drive as `/sample.gif` (CircuitPython fallback mode).

